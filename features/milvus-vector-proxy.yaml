gateway: apigee
schemaVersion: 1.0.0
name: milvus-vector-proxy
displayName: Milvus / Zilliz Cloud Vector Proxy
type: feature
description: Proxies Milvus v2 / Zilliz Cloud vector search REST API with Bearer token authentication, output field selection,
  and vector search parameters.
categories:
- draft
- ai
parameters:
- name: TargetEndpointUrl
  default: https://{milvus.endpoint}/v2/vectordb/entities/search
- name: EnvironmentTier
  default: production
defaultEndpoint:
  name: default
  basePath: /v1/milvus/v2/vectordb
  routes:
  - name: default
    target: default
  flows:
  - name: PreFlow
    mode: Request
    steps:
    - name: KVM-LoadCredentials
    - name: AM-SetMilvusAuth
    - name: JS-FormatMilvusHits
  - name: PostFlow
    mode: Response
    steps: &id001
    - name: JS-FormatMilvusHits
  faultRules: []
defaultTarget:
  name: default
  url: https://{milvus.endpoint}/v2/vectordb/entities/search
  flows:
  - name: PostFlow
    mode: Response
    steps: *id001
  faultRules: []
  httpTargetConnection:
    properties: {}
    url: https://{milvus.endpoint}/v2/vectordb/entities/search
endpoints: []
targets: []
policies:
- name: KVM-LoadCredentials
  type: KeyValueMapOperations
  content:
    keyValueMapOperations:
      metadata:
        name: KVM-LoadCredentials
        continueOnError: 'false'
        enabled: 'true'
        mapIdentifier: milvus-vector-proxy-secrets
      displayName: KVM-LoadCredentials
      properties: {}
      exclusiveCache: 'false'
      expiryTimeInSecs: '300'
      get:
      - metadata:
          assignTo: private.targetApiKey
        key:
          parameter: ApiKey
      - metadata:
          assignTo: private.targetApiSecret
        key:
          parameter: ApiSecret
      scope: environment
- name: AM-SetMilvusAuth
  type: AssignMessage
  content:
    assignMessage:
      metadata:
        name: AM-SetMilvusAuth
        continueOnError: 'false'
        enabled: 'true'
      displayName: AM-SetMilvusAuth
      properties: {}
      set:
        headers:
          header:
          - metadata:
              name: X-Apigee-Feature
            value: milvus-vector-proxy
          - metadata:
              name: X-Apigee-Time
            value: '{system.timestamp}'
      ignoreUnresolvedVariables: 'true'
      assignTo:
        metadata:
          createNew: 'false'
          transport: http
          type: request
- name: JS-FormatMilvusHits
  type: Javascript
  content:
    javascript:
      metadata:
        name: JS-FormatMilvusHits
        continueOnError: 'false'
        enabled: 'true'
        timeLimit: '200'
      displayName: JS-FormatMilvusHits
      properties: {}
      source: "// JS-FormatMilvusHits - Custom logic for milvus-vector-proxy\ntry {\n    var reqContent = context.getVariable(\"\
        request.content\");\n    var reqJson = null;\n    if (reqContent) {\n        try { reqJson = JSON.parse(reqContent);\
        \ } catch (e) {}\n    }\n    \n    // Set execution context metadata\n    context.setVariable(\"milvus-vector-proxy.processed\"\
        , \"true\");\n    context.setVariable(\"milvus-vector-proxy.timestamp\", Date.now().toString());\n    \n    // Response\
        \ processing if in response flow\n    var resContent = context.getVariable(\"response.content\");\n    if (resContent)\
        \ {\n        try {\n            var resJson = JSON.parse(resContent);\n            resJson[\"_apigee\"] = {\n    \
        \            \"feature\": \"milvus-vector-proxy\",\n                \"processedAt\": new Date().toISOString()\n  \
        \          };\n            context.setVariable(\"response.content\", JSON.stringify(resJson));\n        } catch (e2)\
        \ {}\n    }\n} catch (err) {\n    print(\"Error in JS-FormatMilvusHits: \" + err);\n}\n"
resources: []
tests: []
